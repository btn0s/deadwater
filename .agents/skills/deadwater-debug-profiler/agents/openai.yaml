interface:
  display_name: "DEADWATER Debug Profiler"
  short_description: "Debug R3F, shaders, scenes, and performance"
  default_prompt: "Use $deadwater-debug-profiler to reproduce and isolate this DEADWATER bug or bottleneck within its R3F, multipass, raw-color architecture."
