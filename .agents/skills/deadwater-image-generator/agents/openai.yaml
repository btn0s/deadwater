interface:
  display_name: "DEADWATER Image Generator"
  short_description: "Create and prepare DEADWATER image assets"
  default_prompt: "Use $deadwater-image-generator to create or source an image, prepare its PS2 runtime copy, and verify it in DEADWATER."
